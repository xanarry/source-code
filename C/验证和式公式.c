//验证：!p∧(q∨r)
#include <stdio.h>

int main(int argc, char const *argv[])
{
	int z, p, q, r;
	printf("验证合式公式的方法\n\t");
	printf("z = !p∧(q∨r)\n");
	while ((p != 0 &&　q != 1))
	{
		printf("please input p(0 or 1): p = ");
		scanf("%d", &p);
	}
	while (q != 0 && q != 1)
	{
		
	}
	return 0;
}